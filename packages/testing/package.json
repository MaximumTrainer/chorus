{
  "name": "@chorus/testing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shipped test harness: fakes, fixtures, world builders and the boundary rule engine. Maintained with the same care as production code, because acceptance tests are only as trustworthy as these are faithful (CLAUDE.md §4).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@chorus/config": "workspace:*",
    "@types/node": "22.7.5",
    "typescript": "5.6.3",
    "vitest": "2.1.3"
  }
}
