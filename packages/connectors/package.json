{
  "name": "@chorus/connectors",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The connector framework (INT-1): credential encryption, cursor-durable sync, verified and replayable webhooks, rate-limit backoff, health, and normalisation into the Signal envelope. Every connector inherits its correctness from here.",
  "exports": {
    ".": "./src/index.ts",
    "./testing": "./src/testing/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@chorus/core": "workspace:*",
    "@chorus/db": "workspace:*"
  },
  "devDependencies": {
    "@chorus/config": "workspace:*",
    "@types/node": "22.7.5",
    "typescript": "5.6.3",
    "vitest": "2.1.3"
  }
}
